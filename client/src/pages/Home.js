import React from "react";
import Layout from "../components/Layout";

const Home = () => {
  return (
    <div className="custom-scrollbar">
      <Layout>HOME</Layout>
    </div>
  )
};

export default Home;
